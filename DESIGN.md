# 이 사이트를 만드는 규칙

화면을 고칠 때마다 같은 판단을 다시 하고 있었다. 그때마다 내린 결론이 대체로
같았으므로 여기 적어 둔다. 새 화면을 짤 때 읽고, 어긴 자리를 발견하면 고치거나
왜 여기서는 어겨야 하는지를 그 자리에 적는다.

규칙마다 **어겼다가 되돌린 실제 자리**를 함께 적었다. 규칙만 있으면 다음 사람이
같은 실수를 한 번 더 해 봐야 알기 때문이다.

---

## 1. 값이 문장을 대신한다

화면이 자기가 무슨 일을 하는지 해설하기 시작하면 아무도 안 읽는다. 말할 것이
있으면 **셀 수 있는 값으로** 놓는다.

- `2번 남음` → 칸 여섯. 채워진 칸과 빈 칸을 보면 세지 않아도 읽힌다.
- `3 / 6` → 없앰. 위의 칸이 이미 그 말이다.
- `내일 새 문제가 나옵니다` → 줄어드는 시계.
- `연습 판이라 랭킹에 올리지 않습니다` / `개인 기록에는 남습니다` →
  `랭킹 안 올라감`  `개인 기록 남음`. 라벨 하나에 값 하나.

숫자가 반응하게 두는 것도 문장을 대신한다. 없는 이름을 냈을 때 남은 칸이 한 번
튀면, 안 깎였다는 것을 안 깎인 칸이 스스로 말한다.

## 2. 라벨이 앞, 값이 뒤

`새 최고 기록 00:09.80` 꼴이다. 값을 문장 한가운데 끼우지 않는다.

`바로 위와 1곳 차이`는 숫자를 읽고도 `차이`가 나올 때까지 붙들고 있어야 뜻이
닫힌다. `바로 위까지 1곳`으로 고쳤다. `까지`가 이 값을 거리로 만든다 —
`바로 위 00:03`으로 두면 윗줄에 적힌 그 사람의 기록으로 읽힌다.

## 3. 같은 것을 두 번 말하지 않는다

한 화면에서 같은 말이 두 자리에 있으면 읽어야 할 것만 늘어난다. 특히 **이름과
단추**가 같은 말을 하기 쉽다.

- `자꾸 헷갈리는 곳` + `최근 오답 2회` + 단추 `오답 복습` — 한 카드가 자기 이름을
  세 번 댔다.
- `친구 대결` + 단추 `대결` — 아예 같은 낱말.
- `초성 힌트` + `Tab을 누르면 초성이 보입니다`.
- 단추 `힌트`·`모르겠어요`와 그 아래 `Tab 힌트` `Esc 모르겠어요`. 새로운 것은
  키 이름뿐이라 키를 단추에 붙이고 줄을 지웠다.
- `16 / 18` + `도착`. 분수가 이미 다 못 돌았다고 말한다.

카드는 **자기 이름을 한 번만** 댄다. 첫 화면 카드 셋은 맨 윗줄이 이름, 아래가
값, 그리고 카드 전체가 누르는 자리다. 카드 안에 단추를 두면 그 단추에 이름을
적어야 하고, 그러면 이름이 둘이 된다.

## 4. 시키지 않는다

`이름 끝까지 씁니다`는 사실이 아니라 명령이라 건방지다. 시킬 것이 있으면
**누를 것을 준다** — `해운대`를 치면 `해운대시` `해운대군` `해운대구`가 뜨고
끝 글자만 굵다.

옆에서 사실을 말하는 문장(`없는 이름입니다`)과 어울리지 않는 것도 이유다.
한 화면의 말투는 같아야 한다.

## 5. 기능이 못 하는 일을 글로 때우지 않는다

`표지판을 눌러 계속 입력하세요`가 화면에 있었다. 판면 밖을 누르면 초점이
나가고, 그 뒤로는 쳐도 반응이 없기 때문이었다. 문장을 지우고 **치면 잡히게**
만들었다.

안내 문장을 쓰고 싶어질 때는 먼저 묻는다 — 이건 기능으로 풀 수 있는 일인가.

## 6. 한 낱말은 한 뜻만

이 사이트는 같은 낱말을 두 뜻으로 쓴 적이 두 번 있다.

- `시도` — 행정구역(`어느 시도입니까`, `전국 17 시도`)과 trial(`다시 시도`).
  행정구역 쪽이 여덟 자리라 trial 쪽을 옮겼다(`다시 불러오기`).
- `이름` — 닉네임(랭킹·대결)과 지역 이름(`없는 이름입니다`). 약관이 이미 열두
  곳에서 `닉네임`이라 부르고 있어서 그쪽으로 맞췄다.

같은 뜻의 두 낱말을 서로 다른 범위에 붙이는 것도 같은 잘못이다. 계기판이
`전국`(시도 17)과 `대한민국`(전체 245)을 달고 있었는데 둘은 같은 말이고 게다가
앞이 뒤에 포함된다. **낱말이 아니라 종류를 다르게** 두었다 — 위는 곳 이름,
아래는 그것을 담는 그릇(`전체`).

## 7. 크기는 값 몫, 무게는 제목 몫

카드 제목이 500이고 바로 아래 값이 18px 600이라 제목이 제목으로 안 읽혔다.
제목을 600, 값을 500으로 바꾸고 값이 큰 것은 그대로 뒀다.

**계단에서 고른다.** 한때 `xs`부터 `7xl`까지 열한 개를 다 쓰고 있었고, 여덟
쪽짜리 사이트에서 페이지 제목이 여섯 가지 크기였다. 지금은 제목이 `4xl`
하나다(연표의 숫자 제목만 예외 — 그건 낱말이 아니라 숫자다).

읽는 문장은 16px 아래로 내리지 않는다. 라벨과 값은 14px, 꼬리표는 12px까지.
11px 밑은 안 쓴다 — 유일한 예외가 공유 미리보기의 이모지 격자인데, 그건 읽는
글자가 아니라 모양이다.

## 8. 선을 칠하는 색과 면을 칠하는 색은 다르다

토큰 하나가 둘을 겸하면 한쪽이 반드시 안 보인다. `concrete-deep`이 면(손 얹힌
칩, 진행 막대)과 선(테두리, 구분선, 밑줄)을 같이 맡고 있었는데, 면은 바탕에서
한 칸만 떨어지면 되지만 선은 그 정도로는 안 보인다 — 밝은 판에서 대비 1.25였다.
`edge`를 따로 내서 양쪽 3:1로 세웠다.

**대비 기준은 글자와 그림이 다르다.** 글자는 4.5:1, 그림은 3:1이다. 불꽃이
초록이었던 것은 어울리는 노랑이 밝은 판에서 1.29라 안 보였기 때문인데, 기준을
3:1로 다시 잡으니 같은 색조의 어두운 쪽이 섰다.

**면은 반투명으로 칠하지 않는다.** `bg-paint/60`이 안 그래도 작은 차이를 1.26에서
1.15로 또 깎고 있었다. 알파가 상태를 지고 있는 자리는 면 색으로 옮긴다.

## 9. 지도가 주인공이면 지도를 가리지 않는다

지도 위에 얹는 것들(테두리, 이름표)은 **카메라 배율로 나눈다.** 팬 그룹 안에
있으면 지도와 함께 부푼다 — 전국 지도에서 작은 구가 문제인 날은 배율이 스물여섯
배까지 가서 3px 테두리가 팔십 픽셀이 되고 이름표 하나가 도를 덮었다. 지역 경계와
현재 지역 테두리가 이미 `non-scaling-stroke`로 같은 일을 하고 있다.

**창의 비율은 지도 파일이 정하지 않는다.** 전국 viewBox는 울릉이 동쪽 끝을,
서귀포가 남쪽 끝을 밀어 거의 정사각형인데 본토는 그 모양이 아니다. 카메라가
당기고 있으므로 창이 그 비율을 따를 이유가 없다.

**카메라가 질문을 따라간다.** 오늘의 퀴즈 1단계는 `어디쯤인가`를 묻는데 처음부터
정답 지역으로 당겨 두어서, 그 답을 큰 지도가 아니라 구석의 미니맵이 주고 있었다.
전국을 펴 놓고 시도를 맞히면 그 곳으로 들어간다 — **카메라가 움직이는 것 자체가
맞혔다는 신호**라 축하 문구를 놓을 자리가 없다.

## 10. 상자 안에 상자를 넣지 않는다

성격이 다른 것은 자리를 가른다. 오늘의 퀴즈 결과가 한 카드 안에 결과·공유·
카운트다운을 담고 그 안에 공유 카드가 또 있었다. 셋은 각각 오늘 무엇이었나,
지금 할 수 있는 일, 내일이다.

## 11. 기기마다 하고 싶은 일이 다르다

같은 단추를 어디서나 세 개 그리지 않는다.

| 환경 | 1순위 | 2순위 | 3순위 |
| --- | --- | --- | --- |
| 모바일 | 공유 | 카카오톡 | 결과 복사 |
| 데스크톱 | 결과 복사 | 이미지 저장 | (시트가 있으면) 공유 |

데스크톱에서 공유 시트는 없거나, 있어도 무엇이 나올지 사람마다 다르다. 거기서
실제로 하는 일은 복사와 그림 저장이다. 키캡도 자판이 있는 화면에만 붙인다 —
손가락으로 누르는 사람에게 `Tab`은 아무 말도 아니다.

## 12. 색은 뜻을 지고, 그 뜻은 화면마다 같다

- 초록 `sign` — 맞힘, 지금 묻는 곳
- 노랑 `centerline` — 헤맴, 가까움
- 빨강 `alert` — 못 맞힘, 다시 볼 곳
- 회색 `concrete` 계열 — 아직 모르는 곳

색만으로 가르지 않는다. 초록과 빨강은 색각 이상에서 가장 흔히 겹치는 짝이라
빗금과 점을 함께 쓴다.

한 화면에서만 뜻을 줄이지 않는다. 오늘의 퀴즈 칸을 `성공 초록 / 실패 회색`
둘로 줄이자는 안이 있었는데, 노랑이 이미 `가까움`이고 그게 이 게임의 유일한
단서다. 여기서만 둘로 줄이면 같은 판이 화면마다 다른 말을 한다.

## 13. 이모지를 화면에 쓰지 않는다

운영체제마다 다르게 그려져서 어떤 기기에서는 둥근 사탕이 되고 어떤 기기에서는
각진 블록이 된다. 크기와 색을 이쪽에서 못 정하는 것이 더 큰 문제다. 화면은
CSS 타일, **글자밖에 못 싣는 자리(공유 문구)에서만** 이모지를 쓴다.

## 14. 누를 수 있는 것은 누를 수 있게 보인다

브라우저 기본 스타일시트는 `<button>`에 `cursor: default`를 준다. 그것을
뒤집어 주던 Tailwind preflight의 줄이 v4에서 빠졌고, 그 뒤로 이 사이트의 단추
서른일곱 곳이 전부 화살표였다. 한 곳씩 `cursor-pointer`를 붙이지 않고
`globals.css`에서 한 번 세운다.

꺼진 것은 뺀다. 안 눌리는데 손가락이 뜨면 그것이야말로 거짓말이다.

## 15. 이름이 무엇을 주는지 말한다

`랜덤 도전` 하나였다. 겹치는 코스만 걸러 뽑았는데 뒤에 읍면동 코스 이백쉰두
개가 들어오면서 후보의 94%가 읍면동이 됐다. 이름은 `랜덤`인데 시군구를
기대하고 누른 사람은 서른 판을 눌러도 시군구를 못 만난다.

층을 반반으로 뽑는 안도 있었지만, 그러면 **눌러 봐야 무엇이 나오는지 아는
것은 그대로**다. `시군구 랜덤`과 `읍면동 랜덤` 둘로 갈랐다.

목록에 무엇을 더할 때는 그 목록을 쓰는 자리의 이름이 아직 참인지 함께 본다.

## 16. 가운데점을 쓰지 않는다

밖으로 나가는 문장은 줄바꿈과 공백으로 나눈다. `부산 · 9 / 16`은 점 하나가
이름과 숫자 사이에 끼어들어 눈이 한 번 멈추는데, 여백이 같은 일을 소리 없이
한다. (이 문서와 코드 주석은 밖으로 나가는 글이 아니라 예외다.)

## 17. 자료의 한계는 감추지 않고 적는다

`설명하지 말라`는 규칙의 예외다. 자료가 어디까지인지는 설명이 아니라 **값**이다.

경계 자료가 1975~2000년에 5년 단위라, 1981년에 생긴 대구직할시가 1985년 판에서
처음 보인다. 그래서 연표 상세는 큰 숫자를 실제 해로 적고 그 옆에
`1985년 자료에서 확인`을 붙인다. 실제 날짜를 모르면 `자료에 처음 나타난 해`라고
쓴다 — 아는 척하느니 어디까지 아는지를 적는다.

무엇을 세는지도 밝힌다. `2009 20곳 → 2010 22곳`이 전국 총계로 읽히므로
`이 지도 안에서만 셉니다`를 붙였다.

그럴듯한 값을 지어내지 않는다. `최근 5번 중 3번 혼동`이라고 쓸 수 없다 —
분모를 저장하지 않기 때문이고, 지어내느니 아는 것만 적는다.

## 18. 스포일러를 내보내지 않는다

오늘의 퀴즈는 모두가 같은 문제를 푼다. 공유물에 정답이 실리면 받는 사람의 판이
사라진다. 그래서 공유 문구·카카오 카드·공유 그림 어디에도 지역명과 시도를
안 적는다. 화면 안에서는 보여 준다 — 거기서는 자기가 짚은 것이라 스포일러가
아니다.

입력칸의 예시 이름도 정답의 시도 **밖에서** 고른다. 제주는 두 곳, 세종은 한
곳이라 같은 시도에서 뽑으면 그날 답이 반쯤 드러난다.

## 19. 한 줄에 몇이 들어가는지는 가장 좁은 폭에서 센다

첫 화면 `actions`는 1024 이상에서 여섯 열 중 둘이다. 넓은 창인데도 그 칸은
좁다. 여기에 히어로 단추와 랜덤 단추 둘을 `sm:flex-row`로 한 줄에 넣었더니,
랜덤 둘은 `whitespace-nowrap`이라 안 줄고 부족한 폭을 히어로가 다 뒤집어썼다.
한글은 낱말 사이가 아니라 글자마다 끊기므로 `전국 이어하기`가 단추 안에서
세로로 한 글자씩 늘어섰다.

창이 넓다고 그 칸이 넓은 것이 아니다. 격자 칸의 폭으로 센다. 그리고 글자가
결정하는 단추에는 `whitespace-nowrap`을 준다 — 이름이 길어지면 글자가 서는
대신 단추가 넓어져야 한다.

---

## 고칠 때 스스로 물을 것

1. 이 문장이 하는 말을 값으로 놓을 수 있나?
2. 이 말이 이 화면 다른 곳에 이미 있나?
3. 이건 시키는 말인가, 사실을 말하는 말인가?
4. 이 안내는 기능으로 풀 수 있는 일 아닌가?
5. 이 낱말이 이 사이트에서 다른 뜻으로도 쓰이나?
6. 제목이 값보다 무거운가? 크기는 계단에서 골랐나?
7. 선인가 면인가? 글자인가 그림인가? (대비 기준이 다르다)
8. 상자 안에 상자가 있나?
9. 이 색이 다른 화면에서 지고 있는 뜻과 같은가?
10. 이게 밖으로 나가면 남의 판을 망치나?
11. 누를 수 있는 것이 누를 수 있게 보이나?
12. 목록이 늘었는데, 그 목록을 쓰는 자리의 이름이 아직 참인가?
13. 이 줄에 나란히 둔 것들이 **가장 좁은 격자 칸**에서도 나란한가?
